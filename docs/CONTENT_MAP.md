# Content → Motion Map

## RIDER
Use `personalInfo`, hero stats and typing roles. Hero typography stays HTML and readable above the ride scene.

## ENGINE / SYSTEMS
Transform the existing skill categories into a motorcycle/AI system metaphor:
- Python / PyTorch / TensorFlow → compute core
- FastAPI / React / Next.js → control layer
- AWS / Airflow / Databricks / PySpark → infrastructure
- LLM / RAG / Legal-BERT / CV → intelligence layer

## GARAGE
Use existing work and education entries. Each timeline event can activate a garage-style HUD card.

## TRACK
Keep current project descriptions, technologies, metrics and repository URLs. Do not fabricate new metrics.

### MedAI Pro
Visual pipeline: input → preprocessing → CNN → ensemble → Grad-CAM.

### LegalAI
Visual pipeline: document → OCR/parser → clause segmentation → Legal-BERT → RAG → draft.

### GNN Fraud Detector
Visual graph: farmer/land/claim/payment nodes → message passing → anomaly cluster.

### EcomGuard
Visual stream: order → feature engineering → XGBoost → risk score → alert.

### Cyber Most Wanted
Visual stream: crawler → DOM extraction → normalization → intelligence feed.

## FINISH
Use existing contact details, social links, resume CTA and availability status. End the ride with a visual deceleration rather than another generic card grid.
